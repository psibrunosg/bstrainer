{
  "name": "bstrainer",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:seed:build": "node supabase/seed/generate-exercises-seed.mjs",
    "db:reset": "pnpm db:seed:build && supabase db reset",
    "db:deploy": "node scripts/db-deploy.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
